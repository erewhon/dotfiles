# Fedora system packages managed by chezmoi
# Only packages needed BEFORE Homebrew is available.
# Everything else is in the Brewfile.

# Bootstrap essentials
curl
git
zsh

# Build tools (required by Linuxbrew)
gcc
gcc-c++
make
